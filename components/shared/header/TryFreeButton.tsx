import React from "react";
import styles from "./Header.module.css";

const TryFreeButton: React.FC = () => {
  return <button className={styles.tryFreeButton}>Try for Free</button>;
};

export default TryFreeButton;
